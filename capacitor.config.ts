import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dosco.game',
  appName: 'DOSCO',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
``